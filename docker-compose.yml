services:
  db:
    container_name: lunchlog_db
    image: postgres:latest
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust #  This shouldn't be like that for production purposes
    ports:
      - "5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    restart: always
  redis:
    container_name: redis_broker
    image: redis:latest
    ports:
      - "6379"
  app:
    build: .
    container_name: lunchlog_app
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    command: python lunchlog/manage.py runserver 0.0.0.0:8000
    restart: always
    volumes:
      - .:/lunchlog
  # celery:
  #   build: .
  #   command: celery -E --workdir=lunchlog -l error -A config.settings worker -B
  #   container_name: celery_tasks
  #   volumes:
  #     - .:/lunchlog
  #   depends_on:
  #     - app
  #     - db
  #     - redis

volumes:
  postgres_data:
  redis-data:
